export const baseUrl = 'https://localhost:3443/'; //set to the network address where we are running WHAT??? server. This const can be used where ever the server address is needed, if server address ever changes, it only has to be changed here.